{
  "extends": "astro/tsconfigs/strict",
  "include": [
    "next-env.d.ts",
    "pages/**/*",
    "src/**/*", //if it happend in inside src
    "components/**/*", //if it happend inside components
], "compilerOptions": {
    "moduleResolution": "node",
    // 👇️ ... rest
  }
}